# Task Ledger

Durable task registry for all AI Agent sessions.

| ID | Status | Owner | Scope / Owned Paths | Acceptance | Changed Files | Verification | Follow-up |
|---|---|---|---|---|---|---|---|
| B0-001 | DONE | current-session | Repository bootstrap; root files, backend/, web/, docs/ | Base structure exists; backend and frontend build | Root governance, skeleton backend/frontend, Makefile, Docker Compose, CI, contracts, skills | `make fmt`, `make lint`, `make test`, `make backend-build`, `make web-build` pass | B1 toolchain/CI and B3 database foundation |
| B1-001 | DONE | current-session | Toolchain/CI; Makefile, .github/workflows/ci.yml, web/package.json, backend/cmd/api | Reproducible dependency install; backend unit test; CI build | package pinning, health test, npm ci CI | `make fmt`, `make lint`, `make test`, `make backend-build`, `npm ci --include=dev`, `npm run build` pass | B2 contracts; B3 database foundation |
| B2-001 | DONE | current-session | Shared MVP contracts; docs/API_CONTRACT.md, docs/ADR/0001-mvp-boundary.md | API and ownership boundary recorded before feature implementation | API contract, MVP boundary ADR | Read-through complete | B3 database foundation |
| B3-001 | DONE | current-session | MVP database and pure engine; backend/migrations, backend/internal/accounting, backend/queries, backend/internal/db | Database invariants and pure engine tests pass; integration harness passes on VPS | Pure engine types/tests; MVP foundation migration; SQL enforcement/RLS; sqlc.yaml; journal queries; pgx transaction helper; pgx integration harness with transaction-local tenant context | Local `make test` and VPS `TestMVPDatabaseInvariants` (tenant isolation, balance, immutability, idempotency) pass as non-superuser | Generate sqlc code once disk allows; wire HTTP services on top of transaction boundary |
| B3-002 | DONE | current-session | M2 COA/category backend; backend/internal/coa, backend/cmd/api/main.go | COA list/create, deactivate (reject on journal history), category list/create, report-mapping create; RLS via transaction-local `app.tenant_id`; unit tests for validation | backend/internal/coa/{helpers,accounts,categories,report_mappings,helpers_test}.go; backend/cmd/api/main.go (routes only) | `gofmt`, `go vet ./...`, `go test ./...`, `go build ./...` pass | Replace X-Tenant-ID header with JWT context tenant; add integration tests for RLS paths |
| M1-001 | DONE | current-session | JWT auth backend; backend/internal/auth, backend/cmd/api/main.go | Register, login, refresh with HS256 JWT; middleware injects tenant/user context; unit tests for token issue/parse | backend/internal/auth/{auth,helpers,auth_test}.go; backend/cmd/api/main.go | `go vet ./...`, `go test ./...`, `go build ./...` pass | Add refresh-token rotation/revocation table; link tenant membership on register |
| M1-002 | DONE | current-session | M4 reporting backend; backend/internal/reporting, backend/cmd/api/main.go | Trial balance, profit/loss, balance sheet, and cash flow endpoints behind auth middleware | backend/internal/reporting/{handler,helpers}.go; backend/cmd/api/main.go | `go vet ./...`, `go test ./...`, `go build ./...` pass | Add date/period filtering |
| M1-003 | DONE | current-session | Swap tenant source from X-Tenant-ID header to JWT context across cash and coa | All tenant-scoped endpoints read tenant from auth middleware context; no X-Tenant-ID header remains in handlers | backend/internal/cash/{handler,helpers,handler_test}.go; backend/internal/coa/helpers.go; backend/cmd/api/main.go; backend/internal/auth/auth.go | `go vet ./...`, `go test ./...`, `go build ./...` pass | Add refresh-token rotation table |
| M1-004 | DONE | current-session | Refresh-token rotation and revocation; user_tokens migration | Login returns access+refresh; refresh rotates token in same family and revokes old; logout revokes; migration 000002 | backend/internal/auth/auth.go; backend/internal/auth/auth_test.go; backend/cmd/api/main.go; backend/migrations/000002_user_tokens.{up,down}.sql | `go vet`, `go test`, `go build` pass; migration applied on VPS test DB; integration invariants 5/5 pass | Link tenant membership on register |
| M3-001 | DONE | current-session | M3 cash backend; backend/internal/cash, backend/cmd/api/main.go | cash-in/out, transfer, opening balance, reverse with idempotency key, single-transaction journal + chain head + outbox | backend/internal/cash/{handler,journal,helpers,handler_test}.go; backend/cmd/api/main.go | `go vet ./...`, `go test ./...`, `go build ./...` pass | Swap X-Tenant-ID to JWT context; add DB integration test for posting flow |
| M5-001 | DONE | current-session | M1 frontend; web/src, web/package.json | Login/register, onboarding wizard, dashboard, transaction forms, typed API stub, responsive + a11y | web/src/**/*, web/index.html, web/package.json, web/package-lock.json | `npm run build` passes (tsc clean, vite build) | Point API stub at real backend; add frontend tests |
| DEP-001 | DONE | current-session | Production deployment to VPS; Dockerfiles, nginx, docker-compose.prod.yml | api + web + postgres containers up on VPS; app reachable at http://119.28.116.123 | backend/Dockerfile, web/Dockerfile, web/nginx.conf, docker-compose.prod.yml, .env.prod (VPS, not committed) | Verified externally: /healthz ok, frontend 200, register + login success | Disable postgres-test container; add HTTPS; frontend real API integration |
| DEP-002 | DONE | current-session | Frontend auth → backend API (hybrid stub) | register/login/logout in browser hit POST /api/v1/auth/*; dashboard/transactions remain mock until backend endpoints exist | web/src/api.ts | `npm run build` passes; web redeployed on VPS; register/login verified via nginx proxy | Move dashboard/transactions/categories/onboarding to real endpoints; add HTTPS |
| DEP-003 | DONE | current-session | HTTPS-ready reverse proxy (Caddy) + stop postgres-test | Caddy serves :80/:443 and proxies /api to backend and / to web; postgres-test container removed | Caddyfile, docker-compose.prod.yml (caddy service), web/nginx.conf | Verified externally: frontend 200, /healthz ok, API reachable | Add real domain for Let's Encrypt HTTPS |
| DEP-004 | DONE | current-session | Tenant membership on register; frontend wired to real endpoints | Register with tenant_name creates tenant + owner membership; JWT carries tenant_id; login resolves default tenant; frontend accounts/categories/transactions/reports call real API with Bearer token + idempotency, mock fallback on failure | backend/internal/auth/{auth,auth_test}.go; web/src/{api,types,vite.config}.ts | `go test`/`go build` pass; web build pass; e2e verified: register→tenant_id in JWT→accounts/reports 200 | Seed default COA/categories for new tenants; HTTPS domain |
| DEP-005 | DONE | current-session | Seed default COA + categories on tenant registration; production domains in Caddyfile | New tenants get 17 core accounts and 9 categories in the registration transaction; Caddyfile uses tikuma.net/www/accounting.tikuma.net with Let's Encrypt | backend/internal/auth/seed.go; backend/internal/auth/auth.go; Caddyfile | `go test`/`go build` pass; e2e verified via HTTPS: register→17 accounts + categories; tikuma.net & www HTTP 200 HTTPS | Add accounting.tikuma.net DNS A record |
| DEP-006 | DONE | current-session | API end-to-end test via HTTPS: register→cash-in/out→transfer→reports→idempotency→reverse | Full accounting flow works against production: journals POSTED with hash chain, trial balance balanced, idempotent replay returns same journal, reversal marks original VOID + creates REVERSAL with reversal_of_id | backend/internal/cash/journal.go; backend/internal/auth/seed.go (open period); backend/migrations/000004_allow_reversal_link.{up,down}.sql | e2e verified: JRN-2026-000001..006, reports balanced, idempotency replay id 6 returned twice, reversal 6→VOID & 8 REVERSAL reversal_of_id=6 | Seed opening balance flow; closing entries for balance-sheet |
| DEP-007 | DONE | current-session | Balance-sheet includes current-period profit; domain switched to accounting.tikuma.net | Balance sheet now adds profit (revenue − expense) to equity so asset = liability + equity + profit; all four reports consistent | backend/internal/reporting/handler.go; Caddyfile (domain) | Verified via accounting.tikuma.net: P&L profit 289k, balance-sheet balanced true, trial balance 1,011,000 = 1,011,000, cash flow net 289k | Closing entries to move profit to retained earnings at period close |
| DEP-008 | DONE | current-session | Closing entries endpoint (POST /periods/close) | Closing entry posts P&L → 3301 → 3201 (retained earnings) and locks the period; balance-sheet uses correct sign convention for equity/liability | backend/internal/period/{handler,helpers}.go; backend/cmd/api/main.go; backend/internal/reporting/handler.go | e2e verified: close → JRN-2026-000007 PERIOD_CLOSE POSTED, period CLOSED, balance-sheet balanced true (equity 289k), P&L 0, trial balance 1,989,000 | Unlock/reopen flow; opening balance seeding |
| DEP-009 | DONE | current-session | Unlock/reopen period endpoint (POST /periods/unlock) | Reverses the closing entry (PERIOD_REOPEN journal linked to closing) and reopens the period; P&L restored | backend/internal/period/handler.go; backend/cmd/api/main.go | e2e verified: unlock → JRN-2026-000008 PERIOD_REOPEN POSTED, period OPEN, P&L back to 289k, balance-sheet balanced true | Opening balance seeding |
| DEP-010 | DONE | current-session | Opening balance resolve seeded equity; web period UI + onboarding OB fix | equity_account_id now optional — resolves seeded 3101 server-side; frontend added Tutup Buku/Buka Periode buttons and fixed onboarding opening-balance to fetch real account ids | backend/internal/cash/{handler,helpers,handler_test,opening_balance_test}.go; web/src/{api,types}.ts; web/src/components/period.tsx; web/src/screens/DashboardScreen.tsx; web/src/styles.css | `go test`/`go build` + `npm run build` pass; e2e verified: OB with equity_account_id 0 → Dr 1101/Cr 3101, frontend 200 | None blocking |

## Rules

- One task must be claimed before editing.
- Each task declares owned paths and acceptance criteria.
- A task is `DONE` only after verification commands pass.
- Shared contracts require a separate coordination task.
- No commit or push without explicit approval.
