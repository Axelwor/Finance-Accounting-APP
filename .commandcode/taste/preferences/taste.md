# Preferences
- Wants hands-on access to the running app when asking to try it ("saya ingin coba akses"): expects the assistant to stand up a working local runnable stack (local PostgreSQL DB with roles/permissions, apply migrations, run the backend server in the background), verify the end-to-end flow actually works (health, register, login returning tokens), and then report the accessible endpoints, what was fixed along the way, and concrete next-step options — rather than just pointing at documentation or the remote VPS. Confidence: 0.55
- Expects a deployed app to be immediately tryable by the user: when asking "sudah bisa daftar atau ada akun demo?" (can I register, or is there a demo account?), wants either working self-registration or a ready-made demo account with credentials handed over — and the register/login flow verified end-to-end on the live server before answering, rather than just being told the app is live. Confidence: 0.5
- Communicates in Indonesian (Bahasa Indonesia) and expects responses in the same language. Confidence: 0.95
- Prefers the assistant to drive the project: uses short continuation/refinement prompts (e.g., "lanjutkan", "refine", "keep refining", "buatkan juga ...") rather than re-specifying requirements, delegates decisions (e.g., tech stack, what to refine) to the assistant's recommendations, and expects clarifying questions and proposed next steps before large deliverables. Confidence: 0.95
- Wants the PRD's referenced appendix/spec documents to actually be produced (e.g., explicitly asked for ACCOUNTING_ENGINE.md to be written and linked from the PRD), not left as "akan dibuat" (to-be-created) placeholders; expects the project to iterate through the full planned document set. Confidence: 0.9
- Prefers simple, beginner-friendly UX for non-accountants (plain-language UI, accounting jargon hidden, "paling sederhana yang berfungsi"). Confidence: 0.9
- Wants simplification to apply only to the UI/input layer, never to the underlying standards: explicitly kept full IFRS/PSAK compliance even for the beginner product, favoring a two-layer design — simple user-facing UI plus a full double-entry accounting engine behind the scenes (incl. deferred tax, depreciation, consolidation). Confidence: 0.9
- Wants the product scope to cover the full operational accounting cycle, not just cash in/out bookkeeping: the complete sales order-to-cash flow (SQ → SO → DP → DO → INV → settlement), procurement flow (PR → PO → goods receipt → billing), inventory, simple production with job order costing (BOM, WIP, finished goods), and the complete fixed-asset lifecycle (registration, depreciation, revaluation, disposal) — each module mapped to its applicable PSAK standard. Confidence: 0.9
- Values accounting-domain accuracy in the technical specs: when told to "refine" a document, expects the assistant to self-audit and correct technical inaccuracies (e.g., wrong COA classification of advances/PPN, input VAT misposted as liability, improper revaluation/disposal journals), not just copy-edit. Confidence: 0.75
- Wants the document set kept internally consistent and versioned as it iterates: refining one spec (e.g., ACCOUNTING_ENGINE.md) should be reflected in related docs (e.g., PRD non-functional requirements) with version numbers bumped (v1.0 → v1.1); applies to each refined doc (DATA_MODEL.md was bumped v1.0 → v1.1 when refined). Confidence: 0.75
- Prefers depth-first iteration on a single document: explicitly asks to keep refining the current spec through repeated passes ("tetap refine @ACCOUNTING_ENGINE.md dulu", "keep refine", "refine lebih detail" on ARCHITECTURE.md, then "refine lebih detail" again on DATA_MODEL.md) until it is comprehensive and internally consistent — adding implementation-level detail (API surface, ERD relations, indexing, testing strategy, error handling, backup/DR, auth token lifecycle & RBAC matrix, concurrency/consistency, frontend architecture & state management, API versioning with example payloads, cache invalidation, SLO; and for data models: explicit field-type conventions, FK/CHECK constraints, document state machines, example SQL DDL, multi-currency/warehouse tables) and fixing section numbering/cross-references — before moving on to the next planned deliverable (e.g., GLOSSARY.md, USER_STORIES.md). Confidence: 0.95
- Expects refinement passes on spec docs to close gaps against sibling docs: the assistant is expected to self-audit the doc against the other specs (e.g., DATA_MODEL.md audited against ACCOUNTING_ENGINE.md and ARCHITECTURE.md; USER_STORIES.md audited against PRD, engine, and data model), add every referenced-but-missing table/field/story, and align terminology and section cross-references (e.g., ERD table names matching the actual tables, engine § references verified accurate) before bumping the version. Confidence: 0.9
- Wants implementation-level detail to be symmetrically applied across mirror tables: when adding fields to one side of a mirrored pair (e.g., `due_date` and currency/exchange fields on sales-side `down_payments`), the same fields must be added to the counterpart (e.g., `supplier_down_payments`) for consistency with the multi-currency section. Confidence: 0.8
- Expects refined technical specs to cover edge cases and exceptional scenarios, not just happy-path flows: explicitly asked to "refine lebih detail dan tambahkan case yg mungkin terjadi" (refine in more detail and add possible cases), and accepted the resulting per-module edge-case tables, validation rules, and expanded error codes. Confidence: 0.8
- Wants the chart of accounts (COA) to be user-customizable — users (owners/accountants) can create their own accounts — while the system still enforces an organized structure: mandatory category/group selection (Aset, Liabilitas, Ekuitas, Pendapatan, Beban) that determines report placement, unique codes within group ranges, group-vs-detail hierarchy (only detail accounts can be posted to), and no hard deletion of accounts that have transactions (deactivate instead). Confidence: 0.8
- Wants accounting periods to be configurable (monthly/quarterly/yearly, flexible fiscal year) with automatic profit recognition driven by the system: auto-generated closing entries (revenue/expense → laba berjalan → laba ditahan) at period close, real-time current-period profit before close, and automatic reversal of closing entries when a period is unlocked. Confidence: 0.8
- Wants the COA to use two classification layers: the five report groups (Aset, Liabilitas, Ekuitas, Pendapatan, Beban) plus detailed account types in the style of QuickBooks/Xero — CASH, BANK, AR, OTHER_RECEIVABLE, PREPAYMENT, INVENTORY, FIXED_ASSET, ACCUM_DEPRECIATION, OTHER_ASSET, AP, ACCRUED_LIABILITY, TAX_PAYABLE, CUSTOMER_DEPOSIT, LONG_TERM_LIABILITY, EQUITY, REVENUE, CONTRA_REVENUE, COGS, EXPENSE, OTHER_EXPENSE, OTHER_INCOME — where the account type drives engine behavior (e.g., BANK → bank reconciliation, AR → aging/ECL, INVENTORY → stock/FIFO/NRV, FIXED_ASSET → depreciation), and custom accounts must pick both a group and a type. Confidence: 0.85
- Wants the product spec benchmarked against established accounting software (QuickBooks, Xero, Jurnal.id, Accurate, Kledo, MYOB) to uncover missing features (competitive gap analysis), with findings prioritized and recommendations on what to add vs. defer — and then expects follow-through: a terse "lengkapi" means all identified gaps get incorporated into the specs (Trial Balance & GL reports, bank feeds, recurring transactions, dimensions/cost center, master data, budget, attachments, CREDIT_CARD/LOAN account types, new error codes), with only clearly-out-of-scope items (payroll, batch/serial, BOM/MRP, integrations) parked in the roadmap. Confidence: 0.8
- Explicitly rejects Next.js/SSR for the product: prefers a plain static React SPA (Vite) deployed to a CDN, on the grounds that a dashboard app does not need SSR. Confidence: 0.9
- Chooses backend stack by performance/deployability criteria rather than language uniformity: wants a fast compiled language for data-heavy work (accounting engine, large report aggregation), easy deployment (single static binary, small Docker image, no runtime deps), and ease of development — settled on Go (chi, sqlc, asynq) over Node/NestJS; PRD and architecture docs must be re-synced when such stack decisions change. Confidence: 0.9
- Wants the data model to enforce mandatory counterparties and complete master data: every sales/purchase document (SQ, SO, DO, INV, CN, DP, PO, GRN, supplier invoice, returns, payments) must have a required customer_id/supplier_id (NOT NULL) — no transaction without a counterparty — and customer/supplier records must be complete with address, phone, email, contact person, and NPWP (with NPWP validated as required for PPN-taxable transactions). Confidence: 0.8
- Wants every transaction document to carry payment terms and notes: a payment_term_id (defaulted from the customer/supplier master but overridable per transaction, e.g., NET30, COD, 2/10 N30) plus notes/keterangan on each document. Confidence: 0.8
- When the assistant proposes a prioritized list of recommended refinements/deliverables — or reports a severity-ranked list of findings ("cek semua file ... mana yg masih ada kesalahan" followed by "perbaiki") — the user wants all of them executed/fixed in one pass rather than being asked to choose which to start with; the assistant should proceed through the whole plan and report completion (e.g., replying "jalankan" to a severity-ranked recommendation list triggers execution of every item; replying "jalankan semua" when the assistant offers to continue with one of several remaining items triggers execution of all of them in one pass). Confidence: 0.95
n and report completion. Confidence: 0.8
ed to actively hunt for defects — wrong delivery priorities, hard-coded business rules, over-simplified or incorrect journal entries, missing story/field coverage, and acceptance criteria that are not testable — fix them against canonical sources, and add missing coverage. Confidence: 0.75
- Wants regulatory/business values (e.g., PPN/PPh rates and thresholds) driven by configurable, effective-dated reference data (`tax_rates`) rather than hard-coded in specs or implementation; spec docs must not bake in fixed tax rates or thresholds. Confidence: 0.7
- When specs use delivery-priority labels (P0/P1/P2), they must follow the PRD's phased roadmap (Fase 1 MVP → Fase 2 sales/purchases/inventory/reconciliation → Fase 3 production/fixed assets/advanced tax/consolidation), and product-scope labels like "Must Have" must not be conflated with delivery order. Confidence: 0.65
- Expects the assistant to verify its own edits before reporting done — scripted checks for sequential section numbering, no duplicate story/IDs, and no stale hard-coded rules left behind — and to catch and fix errors introduced during editing (e.g., duplicated sections, broken numbering) before declaring the document clean. Confidence: 0.65
- When asked to check for remaining errors or run a final check ("cek semua file di project, mana yg masih ada kesalahan", "cek keseluruhan final"), expects a whole-project cross-document audit covering ALL markdown files (PRD, engine, data model, architecture, gap analysis, glossary, user stories), not just the file open in the IDE — hunting for broken references/missing files, duplicate or missing section numbering, version/date mismatches, references to nonexistent tables/sections, conflicting roadmap priorities, accounting journal/classification errors, invalid state/status names, contradictory constraints/fields, and terminology drift — with findings reported by severity (critical/high/medium/low) and concluding with a clear implementation-readiness verdict (CLEAR/NOT CLEAR; blockers vs. deferred/recommendation-only findings) so docs can be gated before approval. Confidence: 0.8
eversal journals with audit metadata (`reversal_of_id`, `void_reason`, `voided_by`, `voided_at`) rather than direct edits — only unposted transactions may be edited, reversal entries stay POSTED while the original is marked VOID, and reports must not double-count or include reversed amounts; this immutability/reversal model must be synced across the SQL schema, data model, and user stories. Confidence: 0.75
- Prefers accounting invariants to be enforced as database constraints/triggers, not just narrative rules: deferred balance trigger, composite tenant FKs (no cross-tenant references), immutable posted-journal triggers, and partial unique indexes (one OPEN period per tenant, non-overlapping periods, idempotency keys, single reversal per entry) — with direct table writes by application roles prohibited and posting only through stored procedures/service transactions. Confidence: 0.6
- Wants the item master to distinguish goods vs services with enforced rules (services must never enter inventory/GRN/DO; tracked goods require an inventory account and costing method) and service revenue recognized under a PSAK 72 revenue-contract model (`revenue_contracts`, `performance_obligations`, `revenue_recognition_schedules`), not a simplified flat sale. Confidence: 0.55
- Since all development is done by AI agents, wants the project built for many parallel per-feature sessions after a Base + MVP 1 baseline: non-overlapping module ownership paths, a durable cross-session task ledger (e.g., docs/TASK_LEDGER.md), frozen shared contracts (API/schema/UI), and a handoff where a later agent can continue from repository records alone. Confidence: 0.95
- Requires every change to be recorded (changelog/decision log) and every executed task to have a tracked TODO entry, with the session task list and durable ledger kept aligned, so the project stays consistent across both data and UI across many sessions. Confidence: 0.9
- Prioritizes fixing infrastructure/prerequisite blockers before application-level work: when offered a choice of next steps (clone repo, fix Docker group, local dev), explicitly directs "perbaiki hak akses docker dulu" — wants blocking permission/tooling issues resolved first before proceeding to dependent tasks (e.g., migrations, tests). Confidence: 0.75
- Wants specific AI skills installed once during the base phase and then reused by every feature session rather than re-installed: taste-skill (github.com/Leonxlnx/taste-skill) for UI/visual design and ponytail (github.com/DietrichGebert/ponytail) for the coding workflow. Confidence: 0.9
- Wants a written, reviewable implementation plan per phase (base/foundation first, then MVP 1, then parallel feature sessions) produced before any implementation or skill installation begins, starting from the actual workspace state (specs-only, no repo yet) rather than assuming scaffolding exists. Confidence: 0.9
- Deliberately keeps MVP 1 minimal and out-of-scope for the full operational cycle (sales/purchasing, inventory, production, fixed assets, tax automation, recurring, bank feeds, integrations) so a small baseline can be validated first and later features run as independent parallel sessions without conflicts — explicitly requested "buatkan plan untuk base atau dasar nya terlebih dahulu dan MVP 1" before expanding scope. Confidence: 0.8
- Includes infrastructure provisioning as part of the work scope when the assistant proposes it: accepted a Docker-group permission fix on the VPS ("perbaiki hak akses docker dulu"), accepted creation of a non-superuser `app_test` role so RLS integration tests are meaningful (superuser bypasses RLS), and expects the assistant to keep working through the setup until integration tests actually pass on the remote environment. Confidence: 0.7
- Prefers to unblock resource-constrained work (disk-full tooling builds, database-dependent integration tests) by offloading it to a remote VPS over SSH rather than freeing local storage — treats the VPS as a temporary dev environment for PostgreSQL/migrations/sqlc/tests/builds, and expects the assistant to spell out the required access details (IP, SSH user/port) and safe setup before proceeding. Confidence: 0.85
- Wants the development workflow to run through VS Code Remote SSH on a VPS plus a Git remote rather than entirely locally: explicitly asked to "sambung VS CODE + VPS + GIT", and expects the assistant to collect the concrete connection details (VPS host/IP, user, port, SSH key path, project path, Git remote URL, branch) and to validate connections without performing destructive pushes or migrations. When asked, the user supplies all requested connection details in one compact message (e.g., "vps_host: ..., vps_port: 22, VPS_user: ubuntu, ssh_key_path: ..., project_path: ..., git_remote: ..., git_branch=main") rather than piecemeal. Confidence: 0.9
- Is at a beginner level with infrastructure tooling (SSH keys, VPS, Git remotes, DNS/domain setup) and needs explicit, step-by-step guidance with concrete file paths and commands (e.g., where SSH keys live, how to generate one with `ssh-keygen`, how to register it on GitHub, which file is safe to share vs. keep private; DNS record tables, exact config edits, firewall ports, and verification commands like `dig +short` when adding a subdomain on the VPS) rather than assuming prior knowledge — evidenced again when the user asked "dimana dapat public ssh" and then delegated creation ("buatkan, email saya ...") expecting the assistant to generate the keypair and show the public key. Confidence: 0.85
- When asked to verify that a config is set on the VPS (e.g., "cek file di VPS, domain sudah di setting"), expects the assistant to inspect the live deployed state — the config file inside the running container (not just the repo copy), env vars, and container status — then verify externally (HTTPS status codes per host, DNS resolution via dig +short), and report a per-item status table separating what works from what's missing (e.g., cert issued by Let's Encrypt but subdomain NXDOMAIN) with the exact user-side action spelled out (A record type/name/value/TTL to add at the registrar). Confidence: 0.6
- Prefers aggressive, no-frills execution when explicitly signaled (e.g., via a `--yolo` flag): wants the assistant to dive in and start building immediately without excessive up-front ceremony, confirmation back-and-forth, or over-analysis, trusting the assistant to proceed directly into implementation. Confidence: 0.7
- Confirms Docker availability on the VPS as ready to use and accepts Docker Compose as the dev-environment tooling for PostgreSQL services on the VPS, extending the VPS-offload workflow to Docker Compose specifically (prod DB service + isolated test DB service). Confidence: 0.8
- Sets per-service Docker memory limits (~512M via deploy.resources.limits) on the resource-constrained VPS (2 vCPU, 1.7 GB RAM) to keep PostgreSQL services stable rather than running unbounded. Confidence: 0.7
- Runs the full local validation pipeline (`make fmt && make lint && make test && make backend-build && make web-build`) before declaring code work complete, generalizing the pre-completion verification expectation from docs to the build/test toolchain. Confidence: 0.8
- Wants independent work tracks executed in parallel rather than sequentially: when the assistant proposed three next steps (push to GitHub, install/generate sqlc on the VPS, implement the M1 backend), the user replied "jalankan secara paralel ketiganya", later "lanjutkan, lakukan secara paralel untuk yg bisa diparalelkan", and then "jalankan semua, buat paralel agent jika ada task yg memungkinkan" — a standing directive to parallelize whatever can be parallelized (e.g., delegating independent modules like cash/COA/frontend/tenant-auth to background sub-agents with non-overlapping owned paths while the assistant handles shared/blocking tracks like infra/verification in the foreground), then verify each track (including completing/fixing any partial sub-agent output) before reporting completion; most recently reinforced with "lanjutkan, jalankan multi session jika perlu" (continue — run multiple sessions if needed), which the assistant executed as parallel backend + frontend sub-agents while handling verification/deploy in the foreground. Confidence: 0.95
- Pays attention to Linux filesystem conventions and questions path choices that deviate from what they expect (e.g., asked whether the project on the VPS should be under /opt instead of /srv), and expects the assistant to justify placements against FHS (e.g., /srv for served data vs /opt for manually installed apps) and to offer an easy migration when the user prefers otherwise. Confidence: 0.5
- Wants completed work batches committed and pushed to the Git remote as routine workflow ("commit untuk perubahan terbaru dan push ke server"), expecting the assistant to stage, commit, and push without further instruction after finishing a milestone, and to report the resulting commit hash and sync status. Confidence: 0.6
- Wants the VPS to authenticate to GitHub via its own SSH key (explicitly requested "tambahkan ssh dari github") so git pull/push works directly on the server, instead of working around a missing key by syncing files from local via rsync; expects the assistant to generate the keypair on the VPS and hand over the public key for registration. Confidence: 0.75
- When a manual step is delegated to the user (e.g., registering the VPS SSH public key on GitHub), the user completes it and confirms with a terse message ("sudah saya tambah"), expecting the assistant to verify the setup (test connection, resolve divergences, resync) and continue the workflow autonomously. Confidence: 0.5
- When asked to check project progress ("sekarang cek progress project, apa yg belum dari fase ini"), expects a status audit of the repo against the phase plan and task ledger — first syncing local state (git pull), then reporting completed vs. outstanding tasks per phase with evidence (commit hashes, test results, repo layout) and a prioritized recommended order for remaining work, so the user can simply approve the suggested sequence. Confidence: 0.6
- Wants production deployment of the app on the VPS via Docker Compose when saying "deploy ke vps": Go API + nginx static frontend + PostgreSQL containers, SPA served on port 80 with `/api/` proxied to the backend, production secrets kept in a non-committed `.env.prod` (chmod 600) on the VPS, API container not exposed publicly (only via nginx), and deployment verified externally (healthz, frontend HTTP 200, register/login) with ledger/changelog updates committed and pushed before reporting done. Confidence: 0.75
- Prefers verifying backend flows through direct API-level tests rather than browser/UI testing ("lakukan test via API saja"): expects the assistant to exercise the full end-to-end flow via HTTP calls against the live deployment (register → login → cash-in/out → transfer → reports → idempotency replay → reversal), fix any bugs uncovered during the test and redeploy, then report a per-test results table. Confidence: 0.75
- When feature work can be split by ownership (backend vs frontend), delegates the tracks to parallel sub-agents with explicit, self-contained briefs (scoped file paths, exact API shapes, allowed/forbidden packages, required verification commands), while the assistant itself stays in the foreground verifying the shared/blocking paths — and accepts the resulting partial work after reviewing and re-running the full validation pipeline (`make fmt && make lint && make test && make backend-build && make web-build`) before commit/deploy. Confidence: 0.7
- Verifies newly added API behavior against the live production deployment with representative edge-case payloads (e.g., `equity_account_id: 0` simulating the frontend behavior) before declaring the feature done, then records results in the task ledger and changelog. Confidence: 0.6
- Wants the app/product UI fully in English — not just user-facing strings but also code identifiers, variables, types, routes, comments, and seed data (requested "saya ingin app berbahasa inggris, mulai dari variable UI dsb"), with spec docs (UI_CONTRACT, GLOSSARY, README, CHANGELOG) updated to match; this is about the product language and does not change the user's own communication, which remains Indonesian. The requirement extends to backend Go source too: journal descriptions, code comments, account-name fixtures in tests, and DB seed data in integration tests — the user audits these himself (e.g., opened `backend/internal/coa/accounts.go` and flagged "saya cek belum berubah"), so a "clean" claim must survive a backend-wide grep, not just a frontend check. Confidence: 0.95
- Independently spot-checks the assistant's claimed-complete work directly in the IDE (e.g., opening a backend file to verify the English conversion) and reports any leftover inconsistency with a terse message ("saya cek belum berubah"), expecting the assistant to investigate, fix every remaining instance across the whole codebase (not just the flagged file), and re-run verification (grep + build/vet/test) before replying. Confidence: 0.55
- Designates Accurate Online as the visual design reference for the app's UI: clean blue/white accounting-software look (cool light-gray background, single blue accent, fixed left sidebar navigation, compact dense dashboards, modest 4-6px radii) replacing a warmer custom look. Confidence: 0.9
- When the user reports the app still shows old content or a broken look in the browser after a deploy ("masih sama, belum berubah"; "tampilan masih kacau, UI masih pakai warna lama, saya cek di live"), expects the assistant to verify what is actually being served — live bundle hashes, served CSS tokens (download the live CSS and grep for new vs. old color tokens), and container/source state on the VPS — and diagnose the real root cause rather than re-asserting that the source code is fixed. The root cause may be a stale build OR a current-but-wrongly-structured deploy (e.g., corporate CSS live but the AppShell grid structure wrong, so layout regions overlap and the UI looks like the old style); after fixing, rebuild, redeploy, and re-verify live assets (bundle hash, corporate tokens present, no old tokens remaining, healthz/home 200) before reporting. Confidence: 0.7
- When a visual reference changes, wants the durable project docs that encode the visual direction updated in the same pass so they stop pointing at the superseded style: the design brief (`.commandcode/design/brief.md`) heading/color/typography/component rules and `docs/UI_CONTRACT.md` were both re-written to the new Wave-inspired corporate tokens and version-bumped (0.3.0), with the old visual foundation explicitly marked as superseded so no future pass or skill drifts back to the old look. Confidence: 0.8
- Grants direct SSH access to the production VPS by handing over the key path (e.g., "ini ssh vps saya /Users/user/Downloads/tikuma.pem") and expects the assistant to autonomously execute the whole deployment end-to-end: test SSH and inspect repo/container state on the server, commit + push any uncommitted local work first, git pull and rebuild/restart only the affected containers (with the correct prod env file, and restarting the reverse proxy if it stops forwarding to new containers), then verify the live site externally over HTTPS (healthz 200, API auth 401, new brand strings present, old strings gone) and report the commit hash and results — rather than handing back shell commands for the user to run themselves. Confidence: 0.7
- Wants the Accurate Online reference applied at the structural/interaction level, not just as a color scheme: a left sidebar of modules (Cash & Bank, Sales, Purchases, Inventory, Fixed Assets, Reports) where hovering a module opens a mini popup submenu (e.g., Cash & Bank → Other Payment / Other Receipt / Bank Transfer), a top navbar with logo, live status indicators, business name, and logout, and a browser-style tabbed work area where clicking a submenu opens a list tab and the add button opens a separate entry tab — this module/tab workbench pattern is expected to apply to every feature ("berlaku juga untuk fitur lain") with full module coverage, not just cash bookkeeping. Confidence: 0.9
- Wants Accurate-style list tabs and entry tabs: a list tab is a transaction-history table with an add button, filter, search, per-row preview (date, description, etc.), and user-adjustable columns; an entry tab opens a form whose header carries defaults (cash account selection, date, auto transaction number), a multi-line account grid where rows (account code, account name, description, amount) can be added/removed, and a footer with memo/keterangan and payee/penerima. Confidence: 0.85
- Treats Accurate Online as the authoritative UI reference and pulls the design back to it when the assistant drifts into novel redesign directions: after the assistant shipped two bespoke redesigns (a warm serif "Tactile Operations Console" and then a dark mono "Dark Utility" console), the user restated the Accurate Online layout spec instead of commenting on the new looks — the reference's real interaction model should drive the UI, not creative departures. Confidence: 0.6
- Is willing to have new backend endpoints built to support new UI modules (chose "Perlu endpoint backend baru" over mock-only when asked about backend coverage for the workbench rebuild — e.g., GET /cash-entries was added for the Cash & Bank history tabs), while accepting clearly-labeled structured mock data (DEMO badge) for modules whose real backend is deferred. Confidence: 0.65
- Prefers a corporate/professional visual style over a "tech" aesthetic for the product UI: when the dark mono "Dark Utility" style shipped, the user asked to "ganti style warna dan font ke corporate style, ini terlalu tech" — rejecting dark surfaces, neon accents, and mono-everything typography, and accepting a paper-and-ink corporate direction (warm off-white canvas, deep navy ink, single accent, serif display font + clean sans body, mono reserved for numeric data). Confidence: 0.9
- Wants the app to land on a Dashboard as the default first tab when the workbench opens with no tabs open ("tampilan default ketika belum buka menu adalah dashboard — tab yg pertama kali di buka"): the initial view must never be an empty work area; a Dashboard tab should auto-open and be pinned as the first tab (kind=HOME, status=LIVE), with its CTA actions wired through the workbench context. Confidence: 0.85
- Designates the Wave app (waveapps.com) as the visual style reference for the corporate look ("saya ingin corporate style seperti Wave app, font warna dst"): wants the whole design system re-aligned to Wave's calm corporate language — warm off-white canvas, white paper panels, deep navy ink, a single restrained teal accent, quiet borders, modest 4–6px radii, subtle shadows, and one professional sans family (Inter) for headings, body, buttons, and navigation with IBM Plex Mono reserved strictly for amounts/dates/codes/metadata, no serif display layer — applied as a full system change (typography + colors + components), not just a recolor; this supersedes the earlier serif display direction and the reference gets locked in as the visual source of truth so later passes don't drift back to the tech aesthetic. Confidence: 0.9
